{
  "name": "gl-test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "serve": "node scripts/serve.js",
    "build": "zig build && cp zig-cache/wasmtest.wasm web/wasmtest.wasm"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "live-server": "^1.2.1"
  }
}
